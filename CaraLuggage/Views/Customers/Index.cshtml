@model IEnumerable<CaraLuggage.Models.KhachHang>

@{
    ViewBag.Title = "Khách hàng (Admin)";
    Layout = "~/Views/Shared/AdminView.cshtml";
}
<style>
    .crud-btn {
        display: inline-block;
        width: 40px;
        height: 40px;
        border-radius: 100%;
        margin: 2px;
    }

        .crud-btn i {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 100%;
        }

</style>

@{ 
    int count = 0;
}

<div class="row">
    <div class="col-md-12">
        <div class="card strpied-tabled-with-hover">
            <div class="card-header ">
                <h4 class="card-title">Bảng Quản lý Khách hàng</h4>
                @*<p class="card-category">Here is a subtitle for this table</p>*@
            </div>
            <div class="card-body table-responsive">
                <table class="table table-hover table-striped">
                    <thead>
                        <tr>
                            <th>STT</th>
                            <th>
                                @Html.DisplayNameFor(model => model.customer_name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.customer_phone)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.customer_address)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.customer_account)
                            </th>
                            <th>Action</th>
                        </tr>

                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>@count</td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.customer_name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.customer_phone)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.customer_address)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.customer_account)
                                </td>
                                <td style="height: 100%; align-items: center;">
                                    <a href="@Url.Action("Edit", new { id=item.customer_id })" class="crud-btn btn-info">
                                        <i class="fa-solid fa-pen-to-square"></i>
                                    </a>
                                    <a href="@Url.Action("Delete", new { id=item.customer_id })" class="crud-btn btn-danger">
                                        <i class="fa-solid fa-x">
                                        </i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
